# geo2model 原型统一环境（建模 + 图像提取合一）
# 安装：python3 -m venv .venv-gempy && .venv-gempy/bin/pip install -r requirements-geo2model.txt
# 实测版本见技术报告附录（Python 3.14.6）

# --- 三维隐式建模 ---
gempy>=2026.0.3
gempy-viewer>=2026.0.3
pyvista>=0.48
trame
trame-vtk
trame-vuetify

# --- 数值与数据 ---
numpy>=2.5
pandas>=3.0
scipy>=1.18

# --- 图像提取（经典 CV，无深度学习） ---
opencv-python>=5.0
scikit-image>=0.26
scikit-learn>=1.9
PyWavelets

# --- 可视化 ---
matplotlib>=3.11
