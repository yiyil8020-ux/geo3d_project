# GemPy 三维建模环境依赖（与 geomap_demo 图像处理环境分离）
# 安装：
#   python3 -m venv .venv-gempy
#   .venv-gempy/bin/pip install -r requirements-modeling.txt
#
# 实测版本：gempy 2026.0.3 / Python 3.14 (macOS arm64)

gempy>=2026.0.3
gempy_viewer>=2026.0.3
pandas>=2.0
matplotlib>=3.8
numpy>=1.26
scipy>=1.11  # 地形剖面绘制等需要
# 可交互 3D：PyVista 场景 + trame 导出 HTML（model_3d.html）
pyvista>=0.43
trame>=3
trame-vtk
trame-vuetify
nest_asyncio2  # pyvista.export_html 在脚本里启动 trame 所需